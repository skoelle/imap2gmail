# Quell-IMAP (Testkonto bzw. Forward-Ziel)
SOURCE__HOST=imap.example.com
SOURCE__PORT=993
SOURCE__EMAIL=test@example.com
SOURCE__PASSWORD=changeme

# Optional: weiteren Spam-Ordner der Quelle mitrelayten → Gmail [Gmail]/Spam (kein ntfy)
# leer = aus; z.B. 1und1/IONOS: SOURCE__SPAM_FOLDER=Spam
SOURCE__SPAM_FOLDER=

# Gmail-Ziel (App-Passwort: https://myaccount.google.com/apppasswords)
GMAIL__EMAIL=you@gmail.com
GMAIL__APP_PASSWORD=changeme
GMAIL__HOST=imap.gmail.com
GMAIL__PORT=993

# ntfy-Benachrichtigung (leer/gesetzt = aus)
NTFY__TOPIC_URL=
#NTFY__TOPIC_URL=https://ntfy.sh/my-topic

# From-Adressen ohne ntfy (kommagetrennt, case-insensitive)
NTFY__BLACKLIST=
#NTFY__BLACKLIST=user@example.org,someone@example.com

# Spam-Verhalten (Header X-Spam-Flag / X-Spam-Status / X-UI-Filterresults)
# gmail-spam | inbox | skip   (Default: gmail-spam)
SPAM__ACTION=gmail-spam

# Archiv-Regeln → Gmail [Gmail]/All Mail statt INBOX, kein ntfy
# ; = Regeln (OR, erste Treffer gewinnt), | = UND in einer Regel
# subject~/from~ = case-insensitive "enthält"
#ARCHIVE__RULES=subject~Newsletter;subject~Receipt|from~noreply@shop.de
ARCHIVE__RULES=

# Safety-Net-Polling zusätzlich zu IMAP-IDLE
FALLBACK_POLL_SECONDS=60

# ntfy bei Reconnect-Problemen nach X Sekunden (einmal pro Container-Start + Recovery)
RECONNECT__ALERT_SECONDS=3600

STATE_FILE=/data/state.json
